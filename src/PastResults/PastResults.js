import React from 'react';

import './PastResults.css';

const PastResults = (props) => {
    // const teamScores = props.map((teamScore)=> {
        {console.log('ole ',props, ' ')}
    // })
    return (
        <div>
            <div>
                <div>{props}</div>
            </div>
        </div>
    )
}

export default PastResults;